Glass eyebrow pill that sits above a page hero/heading — the `✦ WELCOME BACK` treatment.

```jsx
<Eyebrow icon="sparkles">Welcome back</Eyebrow>
```

`tone`: `violet` (default) / `orange` / `rose` / `neutral`. Keep the text to 1–3 words, uppercase is applied automatically.
